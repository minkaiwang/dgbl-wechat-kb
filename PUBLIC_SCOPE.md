# 公开发布范围

## v0.2.0 纳入范围

v0.2.0 是“代码 + 元数据 + 475 篇授权文字全文”版本。公开内容包括：

- `docs/articles/` 中合集位置 1–475 的 475 篇 Markdown 正文；
- `data/articles.jsonl`、`docs/catalog.md` 和 `docs/llms.txt` 等正文衍生索引；
- 不含正文的轻量元数据、导入状态、质量报告和权利审计；
- 全文 JSONL、元数据 JSONL/CSV、JSON Schema、Manifest 与确定性 Release 构建器；
- 软件代码、测试、依赖锁文件、站点和项目说明。

上述文章正文只覆盖“靓点迷人”拥有或控制权利的原创文字，适用
[`TEXT-LICENSE.md`](TEXT-LICENSE.md) 中的 CC BY-NC 4.0。未来新增文章默认仍为
`pending_owner_review`，不自动继承本版本授权。

## 排除范围

- 原始图片、论文图表、期刊封面、照片、商标、字体、视频和其他第三方素材；
- 原始 HTML、接口响应、抓取日志、失败页面、Cookie、Token 和浏览器配置；
- 私有归档、个人数据、未公开后台材料和无法确认权利归属的内容。

公开 Markdown 只保留图片文字占位符，不包含热链、Base64 图片或原图文件。

## 发布门禁

每次公开提交必须满足：

1. 475 篇文章的作者、许可、位置、稳定 ID、来源 URL、QA 状态和图片占位符计数一致；
2. 当前树与完整 Git 历史不含私有归档、凭据、个人用户路径、个人邮箱或超限文件；
3. 原图和其他第三方素材没有进入 Git 或 Release；
4. 轻量元数据、全文记录、Markdown、目录和导入状态相互对应；
5. Ruff、Pytest、严格知识库审计、Release 确定性构建和 `mkdocs build --strict` 通过；
6. 新增第三方代码、素材或数据有明确来源、许可和署名记录。

远程推送、标签和正式 GitHub Release 是独立的发布动作，候选版本通过本地门禁后仍需维护者
最终确认。
