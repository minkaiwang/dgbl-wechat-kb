# 公开发布范围

## 当前公开版本

v0.1.1 是“代码 + 元数据”版本。它公开 475 条安全元数据和可复现工具，不公开文章全文与图片。

### 纳入

- 软件代码、测试、依赖锁文件和 CI；
- 标题、日期、栏目、稳定 ID、作者字段、微信原文链接等元数据；
- 不含正文的公开目录、质量报告和权利状态清单；
- 项目方法、贡献、安全、引用与许可说明。

### 排除

- `docs/articles/` 中的本地文章正文；
- `data/articles.jsonl`、`docs/llms.txt` 中的正文或摘要片段；
- 原始 HTML、原图、接口快照、抓取日志和浏览器配置；
- Cookie、Token、AppSecret、个人数据和其他凭据；
- 未完成授权核验的第三方材料。

## 发布门禁

每次公开提交必须满足：

1. `scripts/validate_public_release.py` 确认受限路径未被 Git 跟踪；
2. 475 条公开元数据通过字段、数量、顺序、URL 和去重检查；
3. Ruff、Pytest 和 `mkdocs build --strict` 通过；
4. 当前树和完整 Git 历史不含凭据、个人用户路径或受限内容；
5. 新增第三方材料有明确来源、许可和署名记录。

未来公开正文或图片属于新的发布范围，必须逐项完成作者构成与素材权利确认，并另行明确许可。
