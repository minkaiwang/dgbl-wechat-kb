# v0.1.1 公开发布审计

## 发布结论

v0.1.1 的发布范围为“代码 + 475 条安全元数据”。它新增 1 条帖子记录并改进导入与完整性
审计，不提供微信文章正文、摘要片段、图片或私有抓取材料。

用户确认后，版本已发布至
[GitHub Release v0.1.1](https://github.com/minkaiwang/dgbl-wechat-kb/releases/tag/v0.1.1)。
注释标签 `v0.1.1` 指向提交 `494d754fcc9a94d17a68c4b2d33d0f39f16d4bc8`。

## 许可

- 代码：MIT License；
- 公开元数据、目录、报告和项目文档：CC BY 4.0；
- 微信正文、摘要片段、图片与第三方材料：保持原有权利状态，不在开放许可范围内。

## 数据差异

- v0.1.0：474 条，位置 1–474；
- v0.1.1：475 条，位置 1–475；
- 新增：`wx-2247502705-1`，编号 483，发布时间 2026-08-14 16:56:20（UTC+8）；
- 删除：0；
- 原有记录字段变化：0；
- 重复稳定 ID / 来源 URL：0 / 0。

## 发布门禁

- Ruff：通过；
- Pytest：20/20 通过；
- 知识库严格审计：475 条导入状态与 475 篇本地 Markdown 一致，自动复核项为 0；
- 公开发布门禁：66 个候选文件、475 条元数据、475 条状态记录，错误 0；
- 完整 Git 历史：通过受限路径、凭据、用户目录和提交邮箱扫描，错误 0；
- `CITATION.cff`：通过 CFF 1.2.0 schema 验证；
- `mkdocs build --strict`：通过；
- Release JSONL、CSV、JSON Schema、ZIP 与 SHA256：构建通过；
- CSV：UTF-8 BOM、12 列、475 条数据，首末位置 1/475；公式错误扫描为 0，首尾区域
  渲染可读；
- README SVG：渲染通过，版本号、记录数和发布边界无截断；
- 全新隔离 clone：通过；使用 Python 3.12.13 和冻结依赖重建环境，Ruff、20 项测试、
  CFF、严格 MkDocs、资产构建与公开范围审计全部通过；66 个跟踪文件中受限路径为 0；
  五个 Release 资产与工作副本的 SHA256 全部一致；
- 发布后匿名下载与 SHA256：通过；5 个资产均可在不提供 GitHub 凭据时下载，字节数与
  `SHA256SUMS.txt` 全部匹配。

全部发布门禁已完成。

## 远端发布结果

- `main`、本地最终提交与 `origin/main` 一致；
- `v0.1.1` 为正式、非草稿、非预发布 Release，北京时间 2026-08-15 00:31:21 发布；
- JSONL、CSV、JSON Schema、ZIP 与 `SHA256SUMS.txt` 共 5 个资产上传成功；
- GitHub 返回的资产摘要与隔离构建结果一致，匿名 HTTPS 下载复核再次一致；
- 仓库保持 Public，说明已更新为 475 条记录，GitHub Pages 保持关闭。

## 已知边界

- 主页截图同期的 486/474 历史差额仍为 12 篇，编号 483 发布后的主页总数尚未重新核验；
- 编号 3、94、435 未决，356–364 高度疑似一次性跳号，编号 246 有两篇不同文章；
- 正文与图片权利状态仍为待审，不影响本次元数据版本；
- GitHub Pages 和全文发布不在本批次内。
