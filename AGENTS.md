# DGBL 微信公众号知识库协作规则

## 项目目标

本项目把“数字游戏学习研究”公众号的公开文章整理为可追溯、可检索、可审计的 Markdown 知识库。公开仓库保存通过授权与安全检查的原创文字、元数据、索引和代码；原始 HTML、原始图片、抓取日志和未确认许可素材保存在相邻的 `private-archive`，不得提交 Git。

## 权威顺序

1. 用户本轮明确要求。
2. `private-archive/urls/articles.jsonl` 的原始发现清单和 `private-archive/indexes/` 的原始响应快照。
3. `data/articles.jsonl`、文章 frontmatter 和 `reports/` 中的审计记录。
4. 本文件、README 和通用脚本默认值。

## 数据与版权

- 不采集评论者、读者头像、OpenID、登录 Cookie、AppSecret、访问令牌或浏览器配置。
- 每篇文章必须保留原始标题、发布时间、公众号名、原文 URL、稳定文章 ID 和导入状态。
- 不能确认的内容或素材放入 `uncertain` / `pending_review`，不得强行公开许可。
- 当前合集位置 1–475 的原创文字由公众号所有者以网名“靓点迷人”按 CC BY-NC 4.0 授权；未来文章默认待确认。
- 用户原创文字、公开元数据与代码使用不同许可证；第三方论文图表、期刊封面、照片和客座作者内容不得自动继承仓库许可。
- 远程推送、标签、Release、GitHub Pages 和仓库可见性变更前必须取得用户对该动作的最终确认。

## 处理与验收

- 所有批处理必须支持断点续跑、重试、去重和失败清单。
- 原始数据先保存，再生成 Markdown、索引和站点文件。
- 不把“抓取成功”当作“内容保真”；标题、日期、正文长度、图片数、内部链接和随机样本都要核对。
- 每个预期条目最终必须归入 `imported`、`missing`、`duplicate`、`failed` 或 `uncertain`，不得静默遗漏。
- 生成文件可以由脚本覆盖；人工维护文件必须通过补丁修改并保留历史。
